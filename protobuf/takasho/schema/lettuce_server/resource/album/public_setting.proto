syntax = "proto3";

package takasho.schema.lettuce_server.resource.album;

enum PublicSetting {
  PUBLIC_SETTING_UNSPECIFIED = 0;
  PUBLIC_SETTING_ALL_USERS = 1;
  PUBLIC_SETTING_FRIEND_ONLY = 2;
  PUBLIC_SETTING_PRIVATE = 3;
}

